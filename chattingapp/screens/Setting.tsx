import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Setting = () => {
  return (
    <View>
      <Text style={{color: 'red'}}>Setting</Text>
    </View>
  );
};

export default Setting;

const styles = StyleSheet.create({});
